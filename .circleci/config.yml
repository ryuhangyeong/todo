version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.16
    working_directory: ~/repo/frontend
    steps:
      - checkout:
          path: ~/repo
      - run: npm install
      - run: npm run test